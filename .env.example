# COPY this to .env and fill DATABASE_URL (do NOT commit .env)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"
# If frontend needs to call a separate API URL in prod, prefix with VITE_
# VITE_API_URL="https://api.example.com"